const gamesCardsQuery = require('firebase-admin').firestore().collection('games-games');
const cardsDecksQuery = require('firebase-admin').firestore().collection('games-decks');


module.exports = {
};


